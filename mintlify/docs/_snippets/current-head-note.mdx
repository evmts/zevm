> Contract first, `HEAD` second: this page defines the intended ZEVM product contract. As of 2026-03-21, current `HEAD` does not yet satisfy that full contract: `zig build test` is failing, `src/main.zig` only wires `--host` and `--port`, and several documented runtime surfaces remain prototype-only or unwired.
