{
  "schemaVersion": "zevm-active-tests.v1",
  "updated": "2026-04-30",
  "fast": {
    "command": "zig build verify-fast",
    "steps": [
      "zig build test"
    ]
  },
  "verify": {
    "command": "zig build verify",
    "includes": [
      "verify-fast",
      "execution-spec-tests: generated type-0 state fixtures for Paris, Shanghai, and Cancun plus Cancun tx_type_1 access-list",
      "legacy state filler: stExample/invalidTrFiller intrinsic-gas failure",
      "ethereum-tests state: full Cancun stArgsZeroOneBalance, stAttackTest, stBugs, stCallCodes, stChainId, stCodeCopyTest, stCodeSizeLimit, stEIP150Specific, stEIP1559, stEIP158Specific, stEIP2930, stEIP3607, stExample, stHomesteadSpecific, stLogTests, stMemExpandingEIP150Calls, stPreCompiledContracts except quarantined modexpTests oversized-length vectors, stRecursiveCreate, stSLoadTest, stSelfBalance, stStackTests, stStaticFlagEnabled, and stTransitionTest directories across all post forks with state-root and logs-hash assertions",
      "ethereum-tests blockchain fixture smoke: BlockchainTests/ValidBlocks/bcExample/optionsTest.json",
      "execution-spec-tests: generated blockchain fixture structural walk for 10 JSON fixtures",
      "hive rpc-compat: parse and run execution-apis .io cases for chain/network identity, blob base fee, syncing, and unknown-account reads"
    ]
  },
  "hiveSmoke": {
    "command": "zig build hive-smoke",
    "includes": [
      "verify-fast",
      "hive rpc-compat smoke lifecycle with execution-apis forkenv chain-id, lifecycle artifact checks (genesis/chain.rlp/headfcu), and curated phase-1 .io transcript subset"
    ]
  },
  "hiveFull": {
    "command": "zig build hive-full",
    "includes": [
      "verify-fast",
      "hive rpc-compat full lifecycle inventory by discovering all execution-apis/tests/**/*.io files",
      "reports pass/fail and classifies phase-1 out-of-contract transcripts with per-area reasons (block, receipt, logs, call, estimate, transaction submission)"
    ]
  }
}
