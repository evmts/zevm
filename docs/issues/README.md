# Archived Issues Docs

`docs/issues/` is archival history only.

Current normative and active engineering docs are in `docs/specs/`.

Do not use files under `docs/issues/` as source of truth for current ZEVM behavior, progress, or release readiness.
