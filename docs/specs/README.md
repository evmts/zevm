# ZEVM Specs Index

Last updated: 2026-03-30

## Normative contract

The ZEVM normative product/API contract is defined only by:

1. `docs/specs/prd.md`
2. `docs/specs/json-rpc-contract.md`

If wording differs, `json-rpc-contract.md` is authoritative for API-level tuples and payloads.

## Active non-normative traceability maps

- `docs/specs/page-ownership.md`: active Mintlify page traceability ledger (owner role, authoritative sources, required normative anchors, per-page verification status, and verification dates), including deterministic nav reconciliation against `mintlify/mint.json`
- `docs/specs/mintlify-docs-plan.md`: active structure-level maintenance plan aligned to `mintlify/mint.json` and synchronized with `page-ownership.md`

## Supporting docs

- `docs/specs/internal/*`: non-normative summaries that explain the normative contract
- `docs/specs/docs-first-process.md`: process and review gate for keeping docs coherent
- `docs/specs/maintainer-decisions.md`: historical decision log (non-normative)

## Redirect-first process trackers (non-normative)

- `docs/specs/open-questions.md`
- `docs/specs/contradiction-inventory.md`

These remain redirect-first and non-backlog by default. They may contain temporary pending rows only while unresolved, with an explicit as-of pending snapshot (including explicit `none` state).

## Archival notes (non-normative)

The following files are historical references only and are not active normative ledgers:

- `docs/specs/source-evidence-matrix.md`
- `docs/specs/public-docs-sources.md`
- `docs/specs/final-risk-report.md`
